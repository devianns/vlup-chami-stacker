# Vercel/Neon server only. Never prefix this value with VITE_.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Required only when the static game is hosted on GitHub Pages
# and the leaderboard API is hosted on a separate Vercel project.
VITE_API_BASE_URL=https://your-project.vercel.app
